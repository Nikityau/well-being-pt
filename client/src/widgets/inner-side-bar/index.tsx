import React from 'react';

const InnerSideBar = () => {
    return (
        <div className={'inner-side-bar'} id={'inner-side-bar'}>
            inner
        </div>
    );
};

export default InnerSideBar;